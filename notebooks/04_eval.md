# Notebook 04 — Evaluation

## 1. Recovery vs injected truth
>>> Compare `decomp["C"]` to the injected unexplained gap from `data.py`.

## 2. Sensitivity sweep
>>> Loop the threefold decomposition over the five control sets in `docs/04_evaluation.md` §2.

## 3. Variant agreement
>>> Report A-as-reference, B-as-reference, and Neumark-pooled `C` side-by-side.

## 4. Per-role drill-down
>>> Top-10 roles by `abs(C)`. Discuss which roles drive the headline gap.

## 5. Recommendation budget
>>> `from comp_equity.models import recommend_adjustments`
>>> `recs = recommend_adjustments(df)` — total budget, per-role breakdown.

## 6. Conclusions
>>> Draft the Medium-article "Results" section.
